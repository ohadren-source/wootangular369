# Web Framework - FastAPI (replacing Flask)
fastapi==0.104.1
uvicorn==0.24.0

# Database - Turso/SQLite
aiosqlite==0.19.0
libsql-client==0.3.1

# Redis - Agent-to-agent messaging
redis==5.0.0

# HTTP - A2A communication
httpx==0.25.0

# Streaming - SSE for watcher
sse-starlette==1.8.2

# AI - Claude (bumped from 0.25.0 — older SDK rejects tools= on messages.create and predates claude-sonnet-4-5)
anthropic==0.104.1

# Database - PostgreSQL (psycopg2 to match `import psycopg2` in the code; -binary bundles libpq, no system lib needed)
psycopg2-binary>=2.9.9

# Legacy - keeping for migration
flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
gunicorn==21.2.0

# Agent framework (optional - comment out if not available)
# agent-framework>=1.0.0
# agent-framework-a2a>=1.0.0
# a2a>=1.0.0

# Utilities
PyGithub==2.1.1
pyppeteer==1.0.2
trafilatura==1.6.1
