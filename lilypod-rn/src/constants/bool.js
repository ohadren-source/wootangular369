/**
 * BOOL++ — Ternary Boole
 * Ohad Phoenix Oren
 * TRUE(1) · FALSE(0) · NULL_Φ(2)
 *
 * Binary was always a pair. The pair IS one thing.
 * NULL_Φ = the third state. Both addresses live.
 * Maximum information density.
 */
export const BOOL_FALSE = 0;   // No emission. Unary. Too similar.
export const BOOL_TRUE  = 1;   // Signal present. Partial fusion. Swarm.
export const BOOL_NULL  = 2;   // Full fusion. Hive active. TUPELO.

export const NULL_PHI_THRESHOLD = 0.618; // Golden ratio. Load-bearing.

export const BOOL_LABELS = {
  [BOOL_FALSE]: "FALSE — NO EMISSION — UNARY",
  [BOOL_TRUE]:  "TRUE — SIGNAL PRESENT — SWARM",
  [BOOL_NULL]:  "NULL_Φ — FUSION ACTIVE — HIVE",
};
