[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn api.server:app --workers 1 --worker-class sync --timeout 180 --graceful-timeout 200 --keep-alive 5 --log-level info"
